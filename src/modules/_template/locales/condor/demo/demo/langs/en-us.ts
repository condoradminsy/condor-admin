// ❗ 复制此模板后，请替换为实际的类型导入：
// import type { YourModuleSchema } from '@/modules/{your-module}/types/i18n/{your-page}';
interface DemoDemopageSchema {
  name: string;
  status: string;
}

const local: DemoDemopageSchema = {
  name: 'Name',
  status: 'Status'
};
export default local;
